import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ryltsov.alex.file.transfer.demo',
  appName: 'capacitor-file-transfer-bug-403',
  webDir: 'www'
};

export default config;
